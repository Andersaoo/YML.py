# GitLab Configuration
GITLAB_PRIVATE_TOKEN=your_private_token_here# замените на свой токен
GITLAB_URL=https://gitlab.com
GITLAB_GROUP=your-group-name# укажите путь к группе или оставьте пустым для всех проектов
# GITLAB_PROJECT=deployment                      # (опционально) конкретный проект

# Optional settings (можно раскомментировать при необходимости)
 #MAX_PROJECTS=1000
# REQUEST_TIMEOUT=25
# MAX_RETRIES=3
# OUTPUT_DIR=results
# IGNORE_FILES=.gitlab-ci.yml,docker-compose.yml,docker-compose.yaml
# IGNORE_PROJECTS=test-,demo-,example-